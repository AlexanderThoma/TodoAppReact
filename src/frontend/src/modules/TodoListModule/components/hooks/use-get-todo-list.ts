const useGetTodoList = () => {
    const getTodoList = () => {
        
        // call service here

        // return type is the type from the api (responseModel)

        // do validation here
    };
};

export default useGetTodoList;